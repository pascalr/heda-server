<!doctype html>
<html lang="<%- locale %>">
  <head>
    <%- include('partials/head') %>
    <link rel="stylesheet" href="/build/prose_mirror.css">
    <link rel="stylesheet" href="/build/imports.css">
    <link rel="stylesheet" href="/build/toastr.min.css">
    <link rel="stylesheet" href="/auto-complete.css">
    <meta property="og:title" content="HedaCuisine - <%= t('Home_2') %>">
    <meta property="og:type" content="website">
    <meta property="og:url" content="<%= origin %>">
    <meta property="og:description" content="<%= t('Home_1') %>">
  </head>
  <body>
    <noscript>
      <%- disablePreview ? '' : include('../public/ssr/'+locale.toLowerCase()+'/h.html') %>
    </noscript>
    <div class="root" id="root-home"></div>
    <%- include('partials/footer') %>
  </body>
</html>

<script>
  window.gon = <%- JSON.stringify((typeof gon !== 'undefined') ? gon : null) %>
  Promise.all(Array.from(document.images).filter(img => !img.complete).map(img => new Promise(resolve => { img.onload = img.onerror = resolve; }))).then(() => {
    // Wait for images before loading script
    var element = document.createElement("script");
    element.src = "/build/bundle.js";
    document.body.appendChild(element);
});
</script>
