<!doctype html>
<html lang="en">
  <head>
    <%- include('partials/head') %>
    <link rel="stylesheet" href="/build/prose_mirror.css">
  </head>
  <body>
    <%- include('partials/navbar') %>
    <nav class="nav d-flex">
      <div></div>
      <div class="flex-grow-1"></div>
      <% if (user) { %>
        <div class="dropdown">
          <a class="nav-link dropdown-toggle" style="color: black;" href="#" id="dropdownMenuButton" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <img src="/icons/person.svg" height="20"></img>
            <%- user.name %>
          </a>
          <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
            <a href="/todo" class="dropdown-item">Mon profil</a>
            <form action="/logout" method="post">
              <button class="dropdown-item" type="submit">Logout</button>
              <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            </form>
            <hr class="dropdown-divider">
            <h6 class="dropdown-header">Changer d'utilisateur</h6>
            <% users.forEach(function(usr) { %>
              <% if (usr.id == user.id) {return;} %>
              <form action="/change_user" method="post">
                <button class="dropdown-item" type="submit"><%= usr.name %></button>
                <input type="hidden" name="user_id" value="<%= usr.id %>">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
              </form>
            <% }) %>
          </div>
        </div>
      <% } else { %>
        <a href="/login">Login</a>
      <% } %>
    </nav>
    <div id="root"></div>
  </body>
</html>

<script>
  window.mixes = <%- JSON.stringify(mixes) %>
  window.recipes = <%- JSON.stringify(recipes) %>
  window.recipe_kinds = <%- JSON.stringify(recipe_kinds) %>
  window.recipe_ingredients = <%- JSON.stringify(recipe_ingredients) %>
  window.user_tags = <%- JSON.stringify(user_tags) %>
  window.machines = <%- JSON.stringify(machines) %>
  window.favorite_recipes = <%- JSON.stringify(favorite_recipes) %>
  window.suggestions = <%- JSON.stringify(suggestions) %>
  window.recipe_filters = <%- JSON.stringify(recipe_filters) %>
  window.gon = {mixes, recipes, recipe_kinds, recipe_ingredients, user_tags, machines, favorite_recipes, suggestions, recipe_filters}
  window.gon.recipe = {} // FIXME: Ugly fix
</script>
<script src="/build/app.js" type="text/javascript"/></script>
